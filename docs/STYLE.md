# Style — gdrive-mounts

Docs and agent-facing prose here follow `lab`'s house writing standard
(`policy/house-rules.json` → `writing_standard`, TIN-3239): GitLab docs style
plus Google developer documentation style plus plain language. Advisory —
no prose linter is packaged. The older controlled-English aerospace
documentation standard is rejected on record for this house (no license
grant; a look-alike open implementation is blocklisted by name) — nothing
here is branded or tooled after it. Read the full standard in `lab`; this
file only points at it.
